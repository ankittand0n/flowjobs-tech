http://localhost:5173/auth/login?redirect=/dashboard

not redirecting at all

#For your local setup, I'd recommend:
# Run just the required services (Minio and Chrome) in Docker and then run the app locally
docker compose -f compose.dev.yml up minio chrome -d && pnpm dev

